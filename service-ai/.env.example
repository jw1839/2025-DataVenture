# Backend AI Engine 환경 변수 예시
# .env.example 파일을 .env로 복사하여 실제 값으로 채워주세요

# ===== 서버 설정 =====
PORT=8000

# ===== OpenAI API =====
# 발급 방법: https://platform.openai.com/api-keys 에서 'Create new secret key' 클릭
# 용도: GPT-4 (질문 생성, 답변 분석, 평가), Whisper (STT), TTS
OPENAI_API_KEY=sk-xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx
OPENAI_MODEL=gpt-5

# ===== 데이터베이스 (벡터 검색용) =====
# service-core와 동일한 PostgreSQL 사용 (pgvector 확장 필수)
# 형식: postgresql://[사용자]:[암호]@[호스트]:[포트]/[DB명]?schema=public
DATABASE_URL="postgresql://postgres:yourpassword@localhost:5432/flex_recruiter?schema=public"

# ===== 임베딩 모델 =====
# Hugging Face에서 자동으로 다운로드됨 (최초 실행 시)
# 한국어 특화 Sentence-Transformers 모델
EMBEDDING_MODEL=jhgan/ko-sbert-nli

# ===== GCP 설정 (선택) =====
# 발급 방법: service-core와 동일
# GCP_PROJECT_ID=your-gcp-project-id
# GOOGLE_APPLICATION_CREDENTIALS=./gcp-service-account-key.json
